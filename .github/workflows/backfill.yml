name: One-Time Metadata Backfill Verify

on:
  workflow_dispatch:

permissions:
  contents: write 

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: pip install pinecone langchain-huggingface sentence-transformers langchain-text-splitters pdfplumber sqlite3 pandas

      - name: Run Backfill
        run: python brain/backfill_metadata.py

      # - name: Verify Backfill
      #   run: python test/script/verify_backfill.py

      - name: Re-index Pinecone
        env:
          PINECONE_API_KEY: ${{ secrets.PINECONE_API_KEY }}
        run: python brain/reindex_pinecone.py

      # - name: Commit Updated DB
      #   run: |
      #     git config --global user.name "Alpha-Bot"
      #     git config --global user.email "actions@github.com"
      #     git add data/brain_metadata.db
      #     git diff --staged --quiet || (git commit -m "Backfill: Populate metadata for existing PDFs" && git push)
